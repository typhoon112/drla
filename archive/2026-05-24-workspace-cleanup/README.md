# Workspace Cleanup Archive

Created: 2026-05-24

This archive keeps temporary artifacts that should not stay in active experiment directories:

- `cola_download_curl.log`
- `cola_download_nohup.out`
- `download_cola_curl.sh`
- `fake_output.jsonl`
- `fake_summary.json`

These files were useful during download and judge smoke tests, but they are not part of the official experiment evidence. Formal experiment outputs remain under `outputs/`.
